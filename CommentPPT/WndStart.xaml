<Window x:Class="CommentPPT.WndStart"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CommentPPT"
        mc:Ignorable="d"
        Title="WndStart" Height="450" Width="800"
        
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal" 
        TextOptions.TextRenderingMode="Auto"        
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}" WindowStartupLocation="CenterScreen" MinWidth="800" MinHeight="450" Loaded="Window_Loaded"
        >
    <Grid>

        <Button Name="BtnExplorer" Content="浏览"   Margin="50,30,50,350" Click="BtnExplorer_Click" VerticalContentAlignment="Center" VerticalAlignment="Top"  />
        <ListBox Name="LstPPT" BorderThickness="1" BorderBrush="Black"  Margin="50,100,50,20" MinHeight="250" VerticalContentAlignment="Stretch" HorizontalContentAlignment="Stretch" SelectionChanged="LstPPT_SelectionChanged"/>

    </Grid>
</Window>
